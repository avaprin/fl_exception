class UserTG {
  int id;
  String accessHash;
  String firstName;
  String lastName;
  String username;
  String imageUrl;

  UserTG(this.id, this.accessHash, this.firstName, this.lastName, this.username, this.imageUrl);
}
